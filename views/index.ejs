<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Video Chat</title>
    <style>
        body {
            height: 100vh;
            display: flex;
            justify-content: space-between;
        }
        #options {
            flex-basis: 300px;
            padding: 20px;
            border-left: 1px solid #000;
        }
        #video-grid {
            flex-grow: 1;
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            grid-auto-rows: 225px;
            gap: 2%;
            padding: 2%;
            height: 100vh;
            overflow: auto;
        }
        video {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
    </style>
    <script>
        const roomId = "<%= roomId %>";
    </script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" />
    <script src="/socket.io/socket.io.js"></script>
</head>
<body>
    <div id="video-grid"></div>
    <div id="options">
        <p style="font-weight: bold" class="mb-1 mt-2">Controllers</p>
        <button id="muteBtn" class="btn btn-primary">Mute yourself</button>
        <button id="cameraoff" class="btn btn-primary">Turn off camera</button>
        <p style="font-weight: bold" class="mb-1 mt-2">Change Camera</p>
        <select id="selectCam" style="width: 300px; margin: auto" class="form-select" aria-label="Default select example"></select>
        <p style="font-weight: bold" class="mb-1 mt-2">Change Mic</p>
        <select id="selectMic" style="width: 300px; margin: auto" class="form-select" aria-label="Default select example"></select>
        <p style="font-weight: bold" class="mb-1 mt-2">Share Your Screen</p>
        <button id="screenShare" class="btn mt-2 btn-secondary">Share your Screen</button>
    </div>
    <script src="./app.js"></script>
</body>
</html>